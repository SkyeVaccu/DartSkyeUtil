import 'package:skye_utils/util/sqlite/database_table.dart';

///it's the configuration to identity the sqlite
class SqliteConfiguration {
  //the database name
  static const DatabaseName = "";
  //the sign which assign to the login user
  static const IdentitySign = "";
  //the database table list
  static final List<DatabaseTable> databaseTableList = [];
}
