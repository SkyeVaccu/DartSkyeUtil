import 'package:flutter/cupertino.dart';

///it's the abstract initializer to standard all initializer
abstract class Initializer {
  ///init method
  ///@param context : root build context
  void init(BuildContext context);
}
